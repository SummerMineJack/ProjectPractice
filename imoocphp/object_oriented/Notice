1，在PHP中如何打破单继承【使用trait关键字可以实现多继承】
2，当一个对象赋值给另一个变量时
    eg:James1=James；//此时是生成了两个对象，当James回首时，James是不会回收的
       James2=&James；//此时是同一个对象，只是对象名称不同，假如James赋值为null,那么James2也会是为null

3，魔术方法：
    __construct:当某个对象实例化时就会调用，也就是对象的构造方法
    __destruct:当某个对象执行完毕或者赋值为null时会调用
    __toString：当echo 某个对象时，将会自动调用__toString方法进行以字符串的形式进行输出
    __invoke：当对象被当成方法使用时，就会被调用 $obj(4);
    __call:当对象调用了类中没有的方法时，__call方法就会被调用
    __callStatic：当对象调用了类中没有的方法时，__callStatic方法就会被静态调用
    __get：可以操作类中没有定义的属性值等,读取不可访问属性时，会被调用
    __set：可以操作类中没有定义的属性值等,设置不可访问属性时，会被调用
    __isset:可以操作类中没有定义的属性值等,判断某个对象或属性是否已被设置
    __unset:可以操作类中没有定义的属性值等
    __clone:当一个对象被复制时，其中一些属性并不想被赋值就需要调用__clone方法，在方法中进行写死属性的值